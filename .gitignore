data/
# scripts/data/ holds code (the IRS bulk importer etc.), not runtime data —
# re-include it (the directory first, then its .py files) so it stays tracked.
!scripts/data/
!scripts/data/*.py
*.pdf
*.json
__pycache__/
*.pyc
.env
debug/
downloads/
*.png
*.html
logs/

# Diagnostic and one-off scripts (use scripts/ subdirs instead).
# Anchored to the repo ROOT (leading /) so the organized copies under
# scripts/maintenance, scripts/diagnostics, scripts/archive stay tracked.
/check_*.py
/debug_*.py
/probe_*.py
/test_*.py
/diag_*.py
/fix_*.py
!/fix_connections.py
*.bak
*.before_*
/PASTE_THE_FULL_PATH_HERE.txt
/app.zip
/scripts.zip
# Large local DB dump — archived on disk, never committed (exceeds GitHub's limit)
leadflow_backup.sql

blog_drafts/
reels/
landing/
lib/
tests/
Image Assets/

fix_connections.py
run_broward_daily_backfill.ps1
